// Replaced by DashboardLayout, but keeping file to avoid deletion if system checks
import { Outlet } from 'react-router-dom'

export default function Layout() {
  return (
    <main>
      <Outlet />
    </main>
  )
}
