// Replaced by DashboardPage/LoginPage flow, keeping as redirect or empty
import { Navigate } from 'react-router-dom'

const Index = () => {
  return <Navigate to="/" replace />
}

export default Index
